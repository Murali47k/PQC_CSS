[project]
name = "federated-learning"
version = "0.1.0"
description = "Federated learning project using Flower and PyTorch"
requires-python = ">=3.12"

dependencies = [
    "flwr[simulation]>=1.8",
    "torch>=2.0",
    "torchvision>=0.15",
    "numpy",
]